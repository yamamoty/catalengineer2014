<h1>New group</h1>

<%= form_for(@group) do |f| %>
  <h3><%= f.label :name %></h3>
  <%= f.text_field :name, class: "form-control"  %>
  <br>

  <%= f.submit "Create Group", :class=> "btn btn-warning" %>
<% end %>
